#include <stdio.h>

int main(){
    int casos, numero;

    scanf("%d", &casos);

    if(1 <= casos && numero <= casos){
        for(int i = 1; i < casos; i++){
            
        }
    }

    return 0;
}